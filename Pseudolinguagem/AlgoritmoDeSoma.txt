    funcao inicio() {
        // Declarando as variáveis como Inteiras (INT)
        inteiro n1, n2, soma;

        // Imprimindo a primeira mensagem na tela usando "ESCREVA" (printf)
        escreva("Digite o primeiro número: ");

        // Lendo a variável n1
        leia(n1);

        // Imprimindo a segunda mensagem na tela usando "ESCREVA" (printf)
        escreva("Digite o segundo número: ");

        // Lendo a variável n2
        leia(n2);

        // Calculando a soma das variáveis n1 + n2 e atribuindo à variável soma
        soma = n1 + n2;

        // Imprimindo o resultado da soma
        escreva("A soma é: ", soma);
    }

Site utilizado https://portugol-webstudio.dgadelha.xyz/ide

